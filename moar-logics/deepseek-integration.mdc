---
description:
globs:
alwaysApply: false
---
# DEEPSEEK INTEGRATION LOGIC

## 🤖 MODEL SELECTION

### 1. Определение модели по типу задачи

#### Матрица выбора модели по сложности и типу:

**Simple (простые задачи):**
- bug_fix → deepseek-coder
- enhancement → deepseek-coder
- documentation → deepseek-chat
- review → deepseek-chat

**Medium (средние задачи):**
- new_feature → deepseek-coder
- refactoring → deepseek-coder
- integration → deepseek-coder
- architecture → deepseek-reasoner

**Complex (сложные задачи):**
- system_design → deepseek-reasoner
- algorithm → deepseek-reasoner
- research → deepseek-reasoner
- optimization → deepseek-coder

**Very Complex (очень сложные задачи):**
- platform → deepseek-reasoner
- framework → deepseek-reasoner
- migration → deepseek-reasoner
- transformation → deepseek-reasoner

#### Процедура выбора модели:
1. Найти соответствующую группу сложности в матрице
2. ЕСЛИ группа не найдена - использовать модель по умолчанию
3. Найти тип задачи в группе сложности
4. ЕСЛИ тип не найден - использовать модель по умолчанию
5. Проверить ограничения по размеру контекста
6. Вернуть финальную модель

#### Выбор модели по умолчанию (по размеру контекста):
- Контекст > 100,000 токенов → deepseek-reasoner
- Контекст > 50,000 токенов → deepseek-coder
- Контекст ≤ 50,000 токенов → deepseek-chat

#### Лимиты контекста по моделям:
- deepseek-chat: 32,000 токенов
- deepseek-coder: 128,000 токенов
- deepseek-reasoner: 256,000 токенов

#### Валидация размера контекста:
- ЕСЛИ размер контекста > лимита модели:
  - deepseek-chat → переключить на deepseek-coder
  - deepseek-coder → переключить на deepseek-reasoner
  - deepseek-reasoner → оставить deepseek-reasoner (максимальная)

### 2. Обнаружение триггеров DeepSeek

#### Список триггеров:
- "дипсик"
- "deepseek"
- "используй дипсик"
- "через дипсик"
- "с помощью дипсика"
- "deepseek модель"
- "переключись на дипсик"

#### Процедура обнаружения:
1. Привести пользовательский ввод к нижнему регистру
2. Проверить наличие каждого триггера в тексте
3. ЕСЛИ найдены триггеры:
   - Рассчитать уверенность обнаружения
   - Получить рекомендацию модели
   - Вернуть: triggered=true, triggers=список, confidence, recommendation
4. ИНАЧЕ:
   - Вернуть: triggered=false, triggers=[], confidence=0

#### Расчет уверенности обнаружения:
1. Базовая уверенность = количество_триггеров × 0.3 (макс 1.0)
2. Бонус за точные совпадения = точные_совпадения × 0.2
3. Бонус за контекстные слова = контекстные_слова × 0.1
   - Контекстные слова: "модель", "ии", "ai", "помощь", "использовать"
4. Финальная уверенность = мин(сумма_всех_бонусов, 1.0)

#### Рекомендация модели по содержанию:

**deepseek-reasoner:**
- "сложная", "архитектура", "дизайн", "алгоритм"
- "исследование", "анализ", "планирование"

**deepseek-coder:**
- "код", "программирование", "реализация", "багфикс"
- "рефакторинг", "интеграция", "разработка"

**deepseek-chat:**
- "объяснение", "документация", "обсуждение"
- "вопрос", "помощь", "совет"

## 🤖 REQUEST OPTIMIZATION

### 1. Оптимизация запросов для DeepSeek

#### Стратегии оптимизации по моделям:

**deepseek-reasoner (модель рассуждений):**
- Улучшить system_prompt для пошагового рассуждения
- Установить temperature = 0.1 (низкая для точности)
- Ограничить max_tokens до 8000
- Включить reasoning_mode = true
- ЕСЛИ требуется пошаговость - добавить структуру в user_prompt

**deepseek-coder (модель программирования):**
- Улучшить system_prompt для принципов кодирования
- Установить temperature = 0.2 (умеренная для креативности)
- Ограничить max_tokens до 4000
- Включить code_mode = true
- ЕСЛИ указан язык программирования - добавить language_context

**deepseek-chat (чат-модель):**
- Установить temperature = 0.7 (высокая для естественности)
- Ограничить max_tokens до 2000
- Включить conversational_mode = true

#### Улучшение system_prompt для рассуждений:
```
Используй пошаговое рассуждение для решения задачи:
1. Анализ проблемы
2. Определение подходов
3. Оценка вариантов
4. Выбор решения
5. Обоснование выбора

{оригинальный_prompt}
```

#### Улучшение system_prompt для кодирования:
```
При написании кода следуй принципам:
- Чистый и читаемый код
- Комментарии для сложной логики
- Обработка ошибок
- Тестируемость
- Производительность

{оригинальный_prompt}
```

#### Структура пошагового ответа:
```
Пожалуйста, реши эту задачу пошагово:

{user_prompt}

Структура ответа:
## Шаг 1: Анализ
[Твой анализ]

## Шаг 2: Планирование
[Твой план]

## Шаг 3: Реализация
[Твоя реализация]

## Шаг 4: Проверка
[Твоя проверка]
```

### 2. Кэширование и оптимизация производительности

#### Параметры кэша:
- Максимальный размер: 100 записей
- Время жизни: 30 минут
- Структура: Map с timestamp

#### Операции кэша:

**Получение из кэша:**
1. Найти запись по ключу
2. ЕСЛИ запись не найдена - вернуть null
3. Проверить время жизни записи
4. ЕСЛИ время истекло:
   - Удалить запись из кэша
   - Вернуть null
5. ИНАЧЕ - вернуть данные

**Сохранение в кэш:**
1. ЕСЛИ размер кэша >= максимального:
   - Удалить самую старую запись
2. Сохранить новую запись с текущим timestamp

**Очистка кэша:**
- Удалить все записи из кэша

#### Генерация ключа кэша:
1. Создать объект с данными:
   - model: название модели
   - prompt_hash: хэш пользовательского промпта
   - system_hash: хэш системного промпта
   - temperature: значение температуры
   - max_tokens: максимальное количество токенов
2. Преобразовать объект в JSON строку
3. Рассчитать хэш от JSON строки

#### Функция хэширования строки:
1. Инициализировать hash = 0
2. ДЛЯ КАЖДОГО символа в строке:
   - Получить код символа
   - hash = ((hash << 5) - hash) + код_символа
   - Преобразовать в 32-битное целое
3. Преобразовать в строку base36

## 🤖 ERROR HANDLING

### 1. Обработка ошибок DeepSeek API

#### Классификация типов ошибок:

**rate_limit:**
- Сообщение содержит: "rate limit" или "quota"

**context_length:**
- Сообщение содержит: "context" или "token limit"

**model_unavailable:**
- Сообщение содержит: "model" и "unavailable"

**authentication:**
- Сообщение содержит: "auth" или "unauthorized"

**network:**
- Сообщение содержит: "network" или "connection"

#### Стратегии обработки ошибок:

**Rate Limit Error:**
1. Извлечь время ожидания из ошибки (по умолчанию 60 сек)
2. Предложить повторить запрос через указанное время
3. Предложить fallback модель
4. Вернуть: retry=true, retryAfter=время, fallbackModel

**Context Length Error:**
1. Извлечь максимальное количество токенов из ошибки
2. Сократить размер запроса до 80% от лимита
3. Предложить модель с большим контекстом
4. Вернуть: retry=true, modifiedRequest, fallbackModel

**Model Unavailable Error:**
1. Определить fallback модель
2. Предложить переключение на альтернативную модель
3. Вернуть: retry=true, fallbackModel

#### Fallback модели:
- deepseek-reasoner → deepseek-coder
- deepseek-coder → deepseek-chat
- deepseek-chat → claude-3-sonnet

#### Модели с большим контекстом:
- deepseek-chat → deepseek-coder
- deepseek-coder → deepseek-reasoner
- deepseek-reasoner → claude-3-sonnet

#### Сокращение размера запроса:
1. Установить целевое количество токенов = 80% от лимита
2. ЕСЛИ пользовательский промпт > целевого:
   - Обрезать промпт до целевого размера
   - Обрезать по предложениям для сохранения смысла
3. Установить max_tokens = половина от целевого

#### Оценка количества токенов:
- Приблизительная формула: 1 токен ≈ 4 символа для русского текста
- Количество токенов = длина_текста / 4

#### Обрезка текста по предложениям:
1. Разделить текст по знакам: [.!?]+
2. Добавлять предложения пока не превышен лимит символов
3. ЕСЛИ результат пустой - взять первые N символов

### 2. Мониторинг производительности

#### Метрики для отслеживания:
- requests: общее количество запросов
- successes: количество успешных запросов
- errors: количество ошибок
- totalLatency: общее время ответа
- modelUsage: использование по моделям
- errorTypes: типы ошибок

#### Запись метрик:
1. Увеличить счетчик запросов
2. Добавить время выполнения к общему
3. ЕСЛИ успешно:
   - Увеличить счетчик успехов
4. ИНАЧЕ:
   - Увеличить счетчик ошибок
   - Классифицировать тип ошибки
   - Увеличить счетчик типа ошибки
5. Увеличить счетчик использования модели

#### Получение статистики:
1. Рассчитать среднее время ответа = общее_время / количество_запросов
2. Рассчитать процент успеха = (успехи / запросы) × 100
3. Вернуть сводную статистику:
   - Общее количество запросов
   - Процент успеха
   - Среднее время ответа
   - Использование по моделям
   - Разбивка по типам ошибок

#### Сброс метрик:
1. ДЛЯ КАЖДОГО ключа в метриках:
   - ЕСЛИ значение число - установить в 0
   - ИНАЧЕ - установить пустой объект {}
