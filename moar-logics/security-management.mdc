---
description: 
globs: 
alwaysApply: false
---
# SECURITY MANAGEMENT LOGIC

## 🔒 BACKUP SYSTEM

### 1. Автоматическое создание бэкапов

#### Процедура создания бэкапа:
1. Получить текущую дату и время в формате YYYYMMDDHHMMSS
2. Извлечь имя файла из полного пути
3. Сформировать имя бэкапа: `{имя_файла}_{timestamp}.bak`
4. Установить директорию бэкапов: `backup-files`

#### Создание директории бэкапов:
- ЕСЛИ директория `backup-files` не существует:
  - Создать директорию рекурсивно
  - Записать в лог: "Created backup directory: backup-files"

#### Выполнение копирования:
1. Сформировать полный путь бэкапа: `backup-files/{имя_бэкапа}`
2. Скопировать оригинальный файл в путь бэкапа
3. ЕСЛИ копирование успешно:
   - Вернуть: success=true, backupPath, originalPath, timestamp
4. ЕСЛИ ошибка копирования:
   - Вернуть: success=false, error=сообщение_ошибки

### 2. Валидация безопасности операций

#### Проверки безопасности по типам операций:

**DELETE (удаление):**
- backup_created - бэкап создан
- user_confirmation - подтверждение пользователя
- not_system_file - не системный файл
- not_config_file - не конфигурационный файл

**MODIFY (изменение):**
- backup_created - бэкап создан
- syntax_validation - валидация синтаксиса
- not_breaking_change - не критическое изменение

**CREATE (создание):**
- valid_location - корректное расположение
- no_overwrite_existing - не перезаписывать существующие
- proper_permissions - правильные права доступа

#### Процедура валидации:
1. Получить список обязательных проверок для типа операции
2. ДЛЯ КАЖДОЙ проверки:
   - Выполнить соответствующую проверку безопасности
   - ЕСЛИ проверка не пройдена:
     - Вернуть: allowed=false, failedCheck, reason, suggestion
3. ЕСЛИ все проверки пройдены:
   - Вернуть: allowed=true, checksPerformed=количество

#### Реализация проверок безопасности:

**backup_created:**
- Выполнить создание бэкапа файла
- Вернуть результат создания бэкапа

**not_system_file:**
- Проверить, содержит ли путь файла системные паттерны:
  - ".git/", "node_modules/", ".cursor/", "package-lock.json", ".env"
- ЕСЛИ содержит - вернуть: passed=false, reason="System file detected"
- ИНАЧЕ - вернуть: passed=true, reason="Not a system file"

**user_confirmation:**
- В реальной реализации запросить подтверждение пользователя
- Для автоматизации - предполагать подтверждение получено
- Вернуть: passed=true, reason="User confirmation received"

**syntax_validation:**
- Определить расширение файла
- Выполнить валидацию синтаксиса по типу файла
- Вернуть результат валидации

### 3. Управление старыми бэкапами

#### Параметры очистки:
- maxAge = 30 дней (по умолчанию)
- maxCount = 100 файлов (по умолчанию)

#### Процедура очистки:
1. Проверить существование директории `backup-files`
2. ЕСЛИ директория не существует - вернуть сообщение
3. Получить список всех .bak файлов с метаданными:
   - Имя файла
   - Полный путь
   - Дата создания
   - Размер файла
4. Отсортировать по дате создания (новые сначала)

#### Удаление по возрасту:
1. Рассчитать максимальный возраст в миллисекундах
2. ДЛЯ КАЖДОГО файла:
   - Рассчитать возраст файла
   - ЕСЛИ возраст > maxAge:
     - Удалить файл
     - Увеличить счетчики удаленных файлов и размера

#### Удаление по количеству:
1. Получить список оставшихся файлов
2. ЕСЛИ количество > maxCount:
   - Взять файлы после maxCount (самые старые)
   - Удалить эти файлы
   - Обновить счетчики

#### Форматирование размера:
- Конвертировать байты в читаемый формат: Bytes, KB, MB, GB
- Использовать коэффициент 1024 для конвертации

## 🔒 FILE INTEGRITY

### 1. Проверка целостности файлов

#### Процедура проверки целостности:
1. Проверить существование файла
2. ЕСЛИ файл не существует:
   - Вернуть: valid=false, reason="File does not exist", checksum=null
3. Прочитать содержимое файла как UTF-8 текст
4. Рассчитать контрольную сумму SHA-256
5. ЕСЛИ ожидаемая контрольная сумма предоставлена:
   - Сравнить с рассчитанной
   - Вернуть результат сравнения
6. ИНАЧЕ:
   - Вернуть: valid=true, checksum=рассчитанная_сумма

#### Расчет контрольной суммы:
- Использовать алгоритм SHA-256
- Обновить хэш содержимым файла в UTF-8
- Вернуть хэш в шестнадцатеричном формате

### 2. Валидация синтаксиса файлов

#### Поддерживаемые типы файлов:
- .json - JSON валидация
- .md/.mdc - Markdown валидация
- .js - JavaScript валидация
- .ts - TypeScript валидация
- .yml/.yaml - YAML валидация

#### Процедура валидации:
1. Определить валидатор по расширению файла
2. ЕСЛИ валидатор не найден:
   - Вернуть: valid=true, message="No validator available"
3. Прочитать содержимое файла
4. Выполнить специфичную валидацию
5. Вернуть результат валидации

#### JSON валидация:
1. Попытаться распарсить содержимое как JSON
2. ЕСЛИ успешно:
   - Вернуть: valid=true, message="Valid JSON syntax"
3. ЕСЛИ ошибка парсинга:
   - Вернуть: valid=false, message=описание_ошибки, suggestion="Fix JSON syntax errors"

#### Markdown валидация:
1. Разделить содержимое на строки
2. Проверить наличие H1 заголовка (строки начинающиеся с "# ")
3. Проверить незакрытые блоки кода:
   - Найти строки начинающиеся с "```"
   - Проверить наличие закрывающего блока
4. Собрать список проблем
5. ЕСЛИ проблем нет:
   - Вернуть: valid=true, message="Valid Markdown"
6. ИНАЧЕ:
   - Вернуть: valid=false, message=список_проблем, suggestion="Fix Markdown syntax issues"

## 🔒 ACCESS CONTROL

### 1. Контроль доступа к файлам

#### Правила доступа по типам файлов:

**System (системные файлы):**
- Паттерны: ".git/", "node_modules/", ".cursor/rules/"
- Разрешенные операции: ["read"]
- Причина: "System files are read-only"

**Config (конфигурационные файлы):**
- Паттерны: "integration_config.md", "package.json", ".env"
- Разрешенные операции: ["read", "modify"]
- Требует бэкап: true
- Причина: "Configuration files require backup"

**User (пользовательские файлы):**
- Паттерны: "tasks.md", "moar-logics/", "docs/"
- Разрешенные операции: ["read", "modify", "delete", "create"]
- Причина: "User files have full access"

#### Процедура проверки доступа:
1. ДЛЯ КАЖДОГО правила доступа:
   - Проверить соответствие пути файла паттернам правила
   - ЕСЛИ соответствует:
     - Проверить разрешена ли операция
     - Вернуть результат с типом правила и требованиями
2. ЕСЛИ не найдено соответствие:
   - Применить правила пользовательских файлов по умолчанию

### 2. Аудит операций с файлами

#### Структура записи лога:
- timestamp - время операции в ISO формате
- operation - тип операции
- filePath - путь к файлу
- user - имя пользователя (или "AI")
- success - успешность операции
- details - дополнительные детали
- checksum - контрольная сумма (если применимо)

#### Процедура логирования:
1. Создать запись лога с текущими данными
2. Сформировать строку JSON из записи
3. Определить путь к файлу лога: `backup-files/file-operations.log`
4. Создать директорию лога если не существует
5. Добавить строку в конец файла лога
6. ЕСЛИ ошибка записи:
   - Записать ошибку в консоль
   - Вернуть: logged=false, error=сообщение
#### Получение истории операций:
1. Проверить существование файла лога
2. ЕСЛИ файл не существует - вернуть пустую историю
3. Прочитать содержимое файла
4. Разделить на строки и отфильтровать пустые
5. Распарсить каждую строку как JSON
6. Отфильтровать записи по пути файла
7. Взять последние N записей (limit)
8. Отсортировать в обратном порядке (новые сначала)
9. Вернуть историю с количеством записей
