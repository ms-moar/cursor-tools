---
description: 
globs: 
alwaysApply: false
---
# MULTI-CONTEXT SYSTEM LOGIC

## 🎯 CONTEXT MANAGEMENT

### 1. Определение активного контекста

#### Приоритеты определения контекста:

**Приоритет 1: Явное указание в пользовательском вводе**
- Проверить наличие имени контекста в тексте
- Проверить наличие ключа проекта Jira
- Проверить наличие псевдонимов (aliases)
- ЕСЛИ найдено - вернуть: confidence=1.0, reason="Explicit user specification"

**Приоритет 2: Контекст по текущей директории**
- Проверить соответствие текущей директории рабочим директориям контекстов
- ЕСЛИ найдено - вернуть: confidence=0.8, reason="Current directory match"

**Приоритет 3: Контекст по умолчанию**
- Найти контекст с флагом is_default=true
- ЕСЛИ не найден - взять первый контекст из списка
- Вернуть: confidence=0.5, reason="Default context"

#### Процедура обнаружения явного контекста:
1. Привести пользовательский ввод к нижнему регистру
2. ДЛЯ КАЖДОГО контекста:
   - Создать список триггеров:
     - context_name (в нижнем регистре)
     - jira_project_key (в нижнем регистре)
     - aliases (если есть)
   - Проверить наличие каждого триггера в тексте
   - ЕСЛИ найден - вернуть этот контекст

#### Процедура обнаружения по директории:
1. ДЛЯ КАЖДОГО контекста:
   - Получить список рабочих директорий (working_directories)
   - ДЛЯ КАЖДОЙ рабочей директории:
     - Проверить содержит ли текущая директория эту рабочую директорию
     - ЕСЛИ да - вернуть этот контекст

### 2. Переключение между контекстами

#### Процедура переключения:
1. Создать лог переключения с timestamp и причиной
2. Валидировать новый контекст
3. ЕСЛИ валидация не прошла - вернуть ошибку
4. Обновить активный контекст в конфигурации
5. Синхронизировать настройки нового контекста
6. Вернуть результат переключения

#### Валидация контекста:
**Обязательные поля:**
- context_name
- jira_project_key
- jira_login
- jira_status_mapping

**Процедура валидации:**
1. ДЛЯ КАЖДОГО обязательного поля:
   - ЕСЛИ поле отсутствует - вернуть ошибку
2. ЕСЛИ jira_project_key == "NO_NAME":
   - Вернуть ошибку: "Jira project not configured"
3. ИНАЧЕ - вернуть: valid=true

#### Обновление активного контекста:
1. Скопировать текущую конфигурацию
2. Установить activeProjectContext = новый контекст
3. Сохранить обновленную конфигурацию в файл
4. Вернуть обновленную конфигурацию

#### Синхронизация настроек контекста:
1. Проверить настройки Jira:
   - ЕСЛИ jira_project_key != "NO_NAME" - добавить в результат
2. Проверить настройки Confluence:
   - ЕСЛИ confluence_space_key != "NO_NAME" - добавить в результат
3. Проверить настройки GitLab:
   - ЕСЛИ gitlab_project_id != "NO_NAME" - добавить в результат
4. Вернуть список синхронизированных сервисов

## 🎯 CONTEXT-SPECIFIC OPERATIONS

### 1. Контекстно-зависимые операции Jira

#### Создание задачи в контексте:
1. Адаптировать данные задачи под контекст
2. Создать операцию Jira с настройками контекста:
   - project: targetContext.jira_project_key
   - issue_type: targetContext.jira_default_issue_type
   - assignee: targetContext.jira_login
   - labels: ["created-by-ai", "context-{context_name}"]
3. ЕСЛИ jira_component != "NO_NAME":
   - Добавить components: [targetContext.jira_component]
4. ЕСЛИ есть custom_fields:
   - Добавить custom_fields из контекста

#### Адаптация данных задачи под контекст:
1. ЕСЛИ есть title_prefix в контексте:
   - Добавить префикс к заголовку: "{prefix} {title}"
2. Добавить контекстную информацию в описание:
   ```
   {original_description}
   
   ---
   Контекст: {context_name}
   Проект: {jira_project_key}
   ```
3. ЕСЛИ есть default_priority в контексте:
   - Установить приоритет из контекста

#### Получение статуса Jira для контекста:
1. Получить маппинг статусов из context.jira_status_mapping
2. Найти соответствующий статус в маппинге
3. ЕСЛИ найден - вернуть маппированный статус
4. ИНАЧЕ - использовать стандартное маппинг:
   - VAN → "To Do"
   - PLAN → "In Progress"
   - CREATIVE → "In Progress"
   - IMPLEMENT → "In Progress"
   - REFLECT → "In Review"
   - ARCHIVE → "Done"

### 2. Контекстно-зависимые операции GitLab

#### Создание ветки в контексте:
1. ЕСЛИ gitlab_project_id == "NO_NAME":
   - Пропустить создание
   - Вернуть: skipped=true, reason="GitLab not configured"
2. Добавить префикс контекста к имени ветки (если настроен)
3. Создать операцию ветки:
   - project_id: context.gitlab_project_id
   - branch_name: имя с префиксом
   - source_branch: context.gitlab_default_branch

#### Добавление префикса контекста:
- ЕСЛИ есть branch_prefix в контексте:
  - Вернуть: "{branch_prefix}/{branchName}"
- ИНАЧЕ:
  - Вернуть: branchName без изменений

#### Создание Merge Request в контексте:
1. ЕСЛИ gitlab_project_id == "NO_NAME":
   - Пропустить создание
2. Сформировать заголовок: "{context_name}: {task_title}"
3. Создать контекстное описание MR:
   ```
   ## Описание
   {task_description}
   
   ## Контекст
   - Проект: {context_name}
   - Jira: {jiraIssueKey}
   
   {mr_template если есть}
   ```
4. Создать MR с метками: ["context-{context_name}", "ai-generated"]

## 🎯 CROSS-CONTEXT OPERATIONS

### 1. Операции между контекстами

#### Перенос задачи между контекстами:
1. Создать лог переноса с timestamp и контекстами
2. Адаптировать данные задачи для переноса
3. Создать новую задачу в целевом контексте
4. Связать оригинальную и новую задачи
5. Обновить оригинальную задачу с информацией о переносе
6. Вернуть результат переноса

#### Адаптация задачи для переноса:
1. Обновить заголовок:
   - "[Transferred from {fromContext}] {original_title}"
2. Добавить информацию о переносе в описание:
   ```
   {original_description}
   
   ---
   **TRANSFERRED TASK**
   Original Context: {fromContext}
   Original Task: {jiraIssueKey}
   Transfer Date: {current_timestamp}
   ```
3. Добавить метки переноса:
   - "transferred-task"
   - "from-{fromContext}"
   - "to-{toContext}"

#### Связывание перенесенных задач:
- В реальной реализации создать связь в Jira между задачами
- Тип связи: "transferred_to"
- Записать информацию о связи в лог

### 2. Синхронизация между контекстами

#### Синхронизация задач во всех контекстах:
1. Получить список всех контекстов из конфигурации
2. ДЛЯ КАЖДОГО контекста:
   - ЕСЛИ jira_project_key == "NO_NAME" - пропустить
   - Получить задачи для этого контекста
   - Синхронизировать задачи контекста
   - Добавить результат в общий список
3. Вернуть сводку синхронизации

#### Получение задач для контекста:
1. Прочитать содержимое tasks.md
2. Извлечь все задачи из файла
3. Отфильтровать задачи по критериям:
   - jiraProjectKey соответствует контексту ИЛИ
   - contextName соответствует контексту
4. Вернуть отфильтрованный список

#### Синхронизация задач контекста:
1. ДЛЯ КАЖДОЙ задачи в контексте:
   - Получить актуальный статус из Jira
   - Проверить необходимость обновления
   - ЕСЛИ нужно обновление:
     - Выполнить обновление
     - Записать результат как "updated"
   - ИНАЧЕ:
     - Записать результат как "no_change"
   - ЕСЛИ ошибка:
     - Записать результат как "error"
2. Вернуть список всех результатов

#### Проверка необходимости обновления:
1. Получить ожидаемый локальный статус из Jira статуса
2. Сравнить с текущим локальным статусом
3. Получить исполнителя из Jira
4. Сравнить с локальным исполнителем
5. ЕСЛИ есть различия - вернуть список изменений
6. ИНАЧЕ - вернуть: update=false

#### Маппинг статуса Jira в локальный:
1. Создать обратное маппинг из jira_status_mapping контекста
2. Найти локальный статус по Jira статусу
3. ЕСЛИ не найден - вернуть Jira статус как есть

## 🎯 CONTEXT VALIDATION

### 1. Валидация конфигурации контекстов

#### Валидация всех контекстов:
1. Получить список всех контекстов
2. ДЛЯ КАЖДОГО контекста:
   - Выполнить валидацию отдельного контекста
   - Записать результат валидации
3. Определить общую валидность (все контексты валидны)
4. Вернуть сводку валидации

#### Валидация отдельного контекста:
**Обязательные поля:**
- context_name
- jira_project_key
- jira_login

**Процедура валидации:**
1. Проверить наличие обязательных полей
2. ЕСЛИ поле отсутствует - добавить в ошибки
3. Проверить настройки Jira:
   - ЕСЛИ Jira настроена (jira_project_key != "NO_NAME"):
     - ЕСЛИ нет jira_status_mapping - добавить в ошибки
     - ЕСЛИ нет jira_default_issue_type - добавить в предупреждения
4. Проверить рабочие директории:
   - ЕСЛИ есть working_directories:
     - ДЛЯ КАЖДОЙ директории:
       - ЕСЛИ путь некорректен - добавить в предупреждения
5. Вернуть результат: valid=(нет ошибок), errors, warnings

#### Проверка корректности пути:
- Проверить что путь - строка и не пустой
- Проверить что путь не содержит ".." (защита от path traversal)
- Вернуть true если путь корректен
