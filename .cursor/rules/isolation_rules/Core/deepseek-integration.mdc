---
description: 
globs: 
alwaysApply: false
---
# DeepSeek Integration Rule

## Purpose
Автоматическая интеграция с DeepSeek API при упоминании пользователем "дипсик", "deepseek" или запросах на использование DeepSeek модели.

## Trigger Keywords
- "дипсик" (любой регистр)
- "deepseek" (любой регистр) 
- "используй дипсик"
- "через дипсик"
- "с помощью дипсика"
- "deepseek api"

## Activation Protocol

### 1. Configuration Reading
При обнаружении trigger keywords:
1. **ОБЯЗАТЕЛЬНО** прочитать файл `integration_config.json`
2. Извлечь настройки DeepSeek из активного project context:
   - `deepseek_api_key`
   - `deepseek_model` 
   - `deepseek_base_url`

### 2. Context Selection
Если определено несколько project contexts:
1. Спросить пользователя: "Какой проект контекст использовать для DeepSeek?"
2. Предложить варианты из `integration_config.json`
3. Сохранить выбор для текущей сессии

### 3. Model Selection Logic
Автоматически выбрать модель на основе типа задачи:

**deepseek-coder** для:
- Генерация кода
- Отладка и исправление ошибок
- Рефакторинг
- Code review
- Создание скриптов

**deepseek-chat** для:
- Общение и анализ
- Планирование задач
- Документация
- Объяснения концепций
- Переводы

**deepseek-reasoner** для:
- Сложные логические задачи
- Архитектурные решения
- Анализ производительности
- Решение проблем
- Стратегическое планирование

### 4. API Configuration
Использовать параметры из `integration_config.json`:
```
API_KEY: {deepseek_api_key}
MODEL: {deepseek_model}
BASE_URL: {deepseek_base_url}
```

### 5. Error Handling
Если настройки отсутствуют или некорректны:
1. Сообщить пользователю: "Настройки DeepSeek не найдены в integration_config.json"
2. Предложить добавить конфигурацию
3. Показать пример настройки

## Implementation Rules

### Обязательные действия:
1. **ВСЕГДА** читать `integration_config.json` перед использованием DeepSeek
2. **НИКОГДА** не использовать hardcoded API ключи
3. **ВСЕГДА** выбирать подходящую модель автоматически
4. **ОБЯЗАТЕЛЬНО** уведомлять пользователя о выбранной модели

### Формат уведомления:
```
🤖 Использую DeepSeek API:
📋 Контекст: {context_name}
🧠 Модель: {selected_model}
🔧 Задача: {task_type}
```

### Безопасность:
- Никогда не выводить API ключи в логи или сообщения
- Использовать только настройки из `integration_config.json`
- Проверять валидность конфигурации перед использованием

## Integration with Memory Bank Modes

### PLAN Mode
- Модель: `deepseek-reasoner` для сложного планирования
- Модель: `deepseek-chat` для простого планирования

### CREATIVE Mode  
- Модель: `deepseek-chat` для дизайн решений
- Модель: `deepseek-reasoner` для архитектурных решений

### IMPLEMENT Mode
- Модель: `deepseek-coder` для всех задач кодирования

### QA Mode
- Модель: `deepseek-coder` для тестирования кода
- Модель: `deepseek-reasoner` для анализа качества

### REFLECT/ARCHIVE Mode
- Модель: `deepseek-chat` для документации
- Модель: `deepseek-reasoner` для анализа результатов

## Example Usage Flow

1. Пользователь: "Давай сделаем это через дипсик"
2. AI читает `integration_config.json`
3. AI выбирает контекст (если нужно)
4. AI определяет тип задачи и модель
5. AI уведомляет о конфигурации
6. AI выполняет запрос через DeepSeek API

## Priority
**HIGH** - Это правило имеет высокий приоритет при обнаружении trigger keywords.
